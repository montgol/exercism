var function words(str) {
	var wordArray = str.split(" ");
	for (var i = 0, len = wordArray.length; i<len; i++) {
		var wordObj = {};
		for (key in wordObj) {
			if (wordArray[i] == key) {
				wordArray
			}
		}
	}
};